# 项目结构

## 当前真实结构

```text
pixiv-crawl/
├── app/
│   ├── browser/
│   │   ├── client.py
│   │   ├── login.py
│   │   └── state_manager.py
│   ├── core/
│   │   └── config.py
│   ├── crawler/
│   │   ├── artwork_crawler.py
│   │   └── author_crawler.py
│   ├── db/
│   │   └── download_record_repository.py
│   ├── downloader/
│   │   └── image_downloader.py
│   ├── parser/
│   │   └── artwork_parser.py
│   ├── schemas/
│   │   └── artwork.py
│   ├── services/
│   │   ├── cli_service.py
│   │   ├── failure_classifier.py
│   │   ├── failure_exporter.py
│   │   ├── record_exporter.py
│   │   └── task_service.py
│   └── utils/
│       └── file_formatters.py
├── data/
│   ├── exports/              # 导出的失败清单 / 归档记录
│   ├── images/               # 下载图片目录
│   ├── state/                # Playwright 登录态
│   ├── temp/                 # 本地临时调试样本
│   │   ├── html/
│   │   └── json/
│   └── pixiv.db              # SQLite 任务记录
├── tests/
│   ├── test_author_crawler.py
│   ├── test_cli_service.py
│   ├── test_db.py
│   ├── test_downloader.py
│   ├── test_failure_classifier.py
│   ├── test_failure_exporter.py
│   ├── test_main.py
│   ├── test_parser.py
│   ├── test_record_exporter.py
│   └── test_task_service.py
├── Dockerfile
├── docker-compose.yml
├── .dockerignore
├── main.py
├── requirements.txt
├── pyrightconfig.json
├── README.md
├── .env.example
├── 项目构建-环境配置.md
└── 项目结构-代码编写.md
```

## 目录职责

### `app/browser`

负责浏览器和登录态相关能力：

- 启动 / 关闭 Playwright
- 复用 `storage_state.json`
- 自动登录
- `reCAPTCHA` 人工补充验证
- 保存更可读的登录态 JSON

### `app/core`

负责项目统一配置：

- Pixiv 账号密码
- 是否无头模式
- 超时时间
- 图片目录
- 数据库路径
- 日志路径

### `app/crawler`

负责页面采集：

- `artwork_crawler.py`：打开作品详情页、抓取 HTML、保存 HTML / JSON
- `author_crawler.py`：打开作者作品页、提取作者名下作品 ID

### `app/parser`

负责把作品页 HTML 解析成结构化数据：

- 标题
- 作者
- 标签
- 页数
- 图片候选地址
- `__NEXT_DATA__` 调试命中信息

### `app/downloader`

负责图片下载：

- 复用浏览器 cookies 下载
- 自动选原图
- 多图作品自动全下
- 自动判断是否已下载
- 图片按“一个作者一个文件夹”存放
- 文件名按“作品标题 + 作品 ID”生成

### `app/db`

负责 SQLite 任务记录：

- 保存处理状态
- 查询历史
- 筛选失败记录
- 统计状态和错误类型
- 删除旧记录

### `app/services`

负责主流程辅助逻辑：

- `cli_service.py`：终端菜单、输入读取、历史查看、导出、归档
- `task_service.py`：单作品处理、批量任务、作者增量筛选
- `failure_classifier.py`：失败原因分类
- `failure_exporter.py` / `record_exporter.py`：导出失败清单和归档记录

### `app/utils`

负责调试辅助：

- HTML / JSON 格式化

## 当前已经完成的能力

- 自动登录与登录态复用
- 作品详情页抓取
- 作品信息解析
- 多图作品自动下载
- 批量输入作品 ID / 作品链接
- 按作者批量抓取作品
- 按作者增量更新作品
- SQLite 任务记录
- 已完成任务跳过
- 失败任务重试
- 失败原因分类
- 失败清单导出
- 旧记录归档与清理
- Docker 运行支持

## 当前图片保存规则

图片现在按下面的方式保存：

- 一个作者一个文件夹
- 文件名优先用作品标题
- 同时带上作品 ID，避免重名覆盖

示例：

```text
data/images/mignon_24234/引きこもりの妹が制服を着てみた回__142543623.jpg
data/images/mignon_24234/制服まとめ__123456789_p0.jpg
data/images/mignon_24234/制服まとめ__123456789_p1.jpg
```

## `data` 目录现在怎么理解

### `data/images`

正式下载结果目录。  
这是运行产物，不建议上传 GitHub。

### `data/state`

浏览器登录态目录。  
里面的 `storage_state.json` 含有登录 cookies，属于敏感文件，绝对不要上传 GitHub。

### `data/exports`

导出的失败清单和归档文件目录。  
属于运行产物，不建议上传 GitHub。

### `data/temp`

本地临时调试目录。  
主要用来存最近抓下来的 HTML / JSON 方便排查问题。  
当前已经加入 `.gitignore`，不再作为仓库长期样本目录。

### `data/pixiv.db`

SQLite 任务数据库。  
记录每个作品的处理状态、标题、作者、下载数量、错误信息等。  
属于运行产物，不建议上传 GitHub。

## Docker 相关文件

当前仓库已经支持 Docker：

- `Dockerfile`
- `docker-compose.yml`
- `.dockerignore`

推荐用法：

1. 先在宿主机跑通一次登录
2. 生成 `data/state/storage_state.json`
3. 再让容器复用这个登录态运行

原因是 Pixiv 登录可能会触发 `reCAPTCHA`，而容器默认更适合无头运行。

## 上传 GitHub 时建议保留 / 忽略

### 建议保留

- `app/`
- `tests/`
- `README.md`
- `requirements.txt`
- `pyrightconfig.json`
- `Dockerfile`
- `docker-compose.yml`
- `.dockerignore`
- `.env.example`
- 这几份中文说明文档

### 建议忽略

- `.env`
- `.venv/`
- `.idea/`
- `__pycache__/`
- `data/state/`
- `data/images/`
- `data/exports/`
- `data/*.db`
- `data/temp/`
- `logs/`

## 下一步比较合理的方向

- 作者订阅名单
- 标签页 / 排行榜抓取
- 更清晰的统计报表
- 更完整的 Docker 运行说明
- API 方案评估（例如 `pixivpy3`）
