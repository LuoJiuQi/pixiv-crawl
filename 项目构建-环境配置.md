# 项目构建 - 环境配置

这份文档只讲一件事：  
**把项目在一台新机器上跑起来，需要准备什么。**

---

## 一、本地运行需要准备什么

### 1. 安装 Python

建议使用：

- Python 3.12 或 3.13

当前项目依赖 Playwright 和 Pydantic，  
用较新的 Python 版本会更省心。

### 2. 创建虚拟环境

在项目根目录执行：

```powershell
python -m venv .venv
```

### 3. 激活虚拟环境

```powershell
.venv\Scripts\Activate.ps1
```

### 4. 安装依赖

```powershell
python -m pip install --upgrade pip
pip install -r requirements.txt
```

### 5. 安装 Playwright 浏览器

```powershell
playwright install chromium
```

---

## 二、配置文件怎么准备

先把：

```text
.env.example
```

复制成：

```text
.env
```

然后填写你自己的 Pixiv 账号密码。

当前主要配置项有：

- `PIXIV_USERNAME`
- `PIXIV_PASSWORD`
- `HEADLESS`
- `TIMEOUT`
- `DOWNLOAD_DIR`
- `STATE_FILE`
- `DB_PATH`
- `LOG_PATH`

### 推荐配置

本地首次运行时，建议：

```env
HEADLESS=false
```

这样浏览器会弹出来，  
如果 Pixiv 触发 `reCAPTCHA`，你还能手动完成验证。

---

## 三、登录态文件是什么

项目会把登录态保存到：

```text
data/state/storage_state.json
```

这个文件里包含：

- cookies
- localStorage
- 其他登录相关状态

作用是：

- 下次启动时复用登录态
- 避免每次都重新登录

### 注意

这个文件是敏感文件，**不要上传到 GitHub**。

---

## 四、第一次运行推荐顺序

### 方案 A：宿主机直接跑

```powershell
python main.py
```

第一次建议这样跑，因为：

- 可以看到浏览器
- 遇到验证码可以人工处理
- 更适合先把登录态建立起来

### 方案 B：Docker 跑

如果你已经在宿主机上生成过：

```text
data/state/storage_state.json
```

那么就可以用 Docker 复用它。

---

## 五、Docker 环境怎么准备

### 1. 安装 Docker Desktop

新机器如果要走 Docker，先装：

- Docker Desktop

### 2. 准备好这些文件

- 项目源码
- `.env`
- `data/state/storage_state.json`

### 3. 构建镜像

```powershell
docker compose build
```

### 4. 启动容器

```powershell
docker compose run --rm pixiv-crawl
```

当前容器会挂载：

- `./data`
- `./logs`

所以容器里产生的下载图片、数据库、登录态仍然会保存在宿主机。

---

## 六、Windows 和 Docker 的一个小差别

`requirements.txt` 里已经做了平台兼容：

- `win32_setctime` 只会在 Windows 安装
- Linux Docker 容器里不会再因为这个依赖报错

---

## 七、哪些文件不要上传

这些都不应该上传 GitHub：

- `.env`
- `data/state/`
- `data/images/`
- `data/exports/`
- `data/*.db`
- `data/temp/`
- `logs/`

---

## 八、环境是否正常的最小检查

如果想确认环境装好了，可以跑：

```powershell
python -m unittest tests.test_main tests.test_parser tests.test_downloader -v
```

如果这些都能过，通常说明：

- Python 环境正常
- 依赖安装正常
- 项目代码至少能通过基础回归检查
